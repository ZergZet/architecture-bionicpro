
# Задание 1. Повышение безопасности системы

Папка Task1\

## Задача 1. Архитектурное решение для управления учётными данными пользователя. 

Папка Task1\Job1BFF\

Решение должно учитывать и обеспечивать следующие аспекты:
- Унификацию доступа в системе BionicPRO. Это будет осуществляться через запрос данных учётных записей из внешнего источника, который расположен в стране представительства компании. Принципы локального хранения персональной и медицинской информации не должны быть нарушены.
    - Добавлен Keycloak
    - База пользователей и ролей (без медицинских данных)
    - Добавлен внешний IdP страны представительства

- Безопасную схему работы с access- и refresh-токенами, которая исключает передачу фронтенду токенов, которые были получены от IdP.
    - Добавлен в BFF, реализующий паттерн **«Прокси для токенов»** на **PKCE (S256)**.
- Для возможность поддержки аутентификации пользователей через различные внешние удостоверяющие службы, действующие в разных странах.
    - добавлен внешний IdP подключённой страны

## Задача 2. Улучшение безопасности существующего приложения, заменой Code Grant на Authorization Code Flow + PKCE:

Папка Task1\Job2BFF\
 - так как frontend-это web- приложение, то добавлен сервис BFF, который рализует **«Прокси для токенов»** на **PKCE (S256)**, через который взаимодействует c фронтендом и храненит токены от keycloak.

# Задание 2. Разработка сервиса отчётов

Папка Task2\

## Задача 1. Архитектура решения для подготовки и получения отчётов. 

Папка Task2\Job1\

1. На архитектурную схему добавлены
    - Сервис отчётов
    - Apache Airflow 
    - Витрина отчётов (ClickHouse)

## Задача 2. Разработан Airflow DAG и настроен на запуск по расписанию.

Папка Task2\Job2\

1. Сгенерирована база CRM 
 - ФИО, email, имя пользователя, тип протеза, модель протеза, серийный номер, номер заказва, цена заказа, дата установки.
2. Сгенерирована база телеметрии бионических протезов PostgreSQL
 - серийный номер, тип события, время события, время отклика, заряд батареи, качество сигнала, код ошибки 
3. Спроектирована структура витрины данных ClickHouse для выстрого доступа к данным пользователя. Аналитика сгруппирована по телеметрии в разрезе клиентов.
4. Разработан Airflow DAG для объединения данных телеметрии и CRM
5. Настроено расписание сбора данных и подготовки витрины

docker-compose с контейнерами для:
1. база CRM (postgres)
2. база телеметрии (postgres)
3. витрины данных (ClickHouse)
4. Apache Airflow + Airflow_DB (postgres)

## Задача 3. Создание бэкенд-части приложения для API.

Папка Task2\Job3\

1. Добавлен API /reports в этот бэкенд для передачи отчётов, который будет возвращать подготовленный отчёт по заданному пользователю. 
2. Отчёт запрашивается из OLAP-базы без необходимости выполнять сложные вычисления в реальном времени.

в docker-compose добавлены контейнер для бэкенда

## Задача 4. Реализуйте ограничение доступа к эндпоинту отчётности.
## Задача 5. Добавлена UI кнопку получения отчёта и вызова эндпоинта его генерации.

Папка Task2\Job5BFF\

В docker-compose добавлены контейнеры для:
1. Keyclock
2. frontend
3. сервис BFF, реализующий паттерн **«Прокси для токенов»** на **PKCE (S256)**.

- Настроен доступ к отчёту по пользователю,  для предоставления только в отношении себя. 
- Добавлен тестовый клиент с password grant для прогона тестов(подробно readme в папке).
